<form action="/account/signin" method="post">
  <div class="card shadow border-0 rounded-4 mt-5" style="max-width: 500px; margin: auto;">
    <h5 class="card-header text-center bg-primary text-white fw-bold">Sign In</h5>
    <div class="card-body">
      {{#if disabled}}
      <div class="alert alert-danger">
        Tài khoản của bạn đã bị vô hiệu hóa. Vui lòng liên hệ quản trị viên.
      </div>
      {{else if error}}
      <div class="alert alert-warning alert-dismissible fade show" role="alert">
        <strong>⚠️ Error.</strong> Invalid username or password.
        <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
      </div>
      {{/if}}

      {{#if success}}
      <div class="alert alert-success alert-dismissible fade show" role="alert">
        🎉 Account created successfully! Please sign in now.
        <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
      </div>
      {{/if}}

      <div class="mb-3">
        <label for="txtUsername" class="form-label">Username</label>
        <input type="text" class="form-control" id="txtUsername" name="username" placeholder="Enter your username"
          required autofocus />
      </div>

      <div class="mb-3">
        <label for="txtPassword" class="form-label">Password</label>
        <input type="password" class="form-control" id="txtPassword" name="password" placeholder="Enter your password"
          required />
      </div>
    </div>

    <div class="card-footer text-center bg-light">
      <button type="submit" class="btn btn-primary px-4 fw-bold">
        <i class="bi bi-box-arrow-in-right"></i> Sign In
      </button>
    </div>

    <div class="mt-3 text-center">
      <button type="button" id="googleLoginBtn" class="btn btn-danger px-4 fw-bold">
        <i class="bi bi-google"></i> Sign in with Google
      </button>
    </div>
  </div>
</form>

<script>
  document.getElementById('googleLoginBtn').addEventListener('click', async () => {
    try {
      // 1. Gọi API login-demo để lấy JWT
      const res = await fetch('/api/auth/login-demo', {
        method: 'POST'
      });

      if (!res.ok) {
        alert('Login demo failed');
        return;
      }

      const data = await res.json();

      // 2. Lưu JWT (demo)
      localStorage.setItem('jwt_demo', data.token);

      // 3. Thông báo thành công
      alert('Google Login (Demo) thành công.\nJWT đã được lưu.');

      // (tuỳ chọn) chuyển trang test
      window.location.href = '/account/jwt-test';
    } catch (err) {
      alert('Error calling JWT demo API');
    }
  });
</script>